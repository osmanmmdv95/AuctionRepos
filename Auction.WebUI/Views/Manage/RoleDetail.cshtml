@model Auction.WebUI.ViewModels.Manage.RoleViewModel

@{
    ViewData["Title"] = "RoleDetail";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Rol Detayları</h2>
<hr />
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">Kullanicilar</dt>
        <dd class="col-sm-10">@ViewBag.UsersInRole</dd>
    </dl>
</div>
<br />
<div>
    <a asp-controller="Manage" asp-action="EditRole" asp-route-id="@Model.Id" class="btn btn-sm btn-success">Düzenle</a> |
    <a asp-action="Roles">Geri Dön</a>
</div>
